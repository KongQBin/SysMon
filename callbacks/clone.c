#include "callbacks.h"

long cbClone(CB_ARGVS)
{
//    cbDoS(pid,regs);
    return 0;
}

long ceClone(CB_ARGVS)
{
//    ceDoS(pid,regs);
//    if(CALL(regs) & DoS())
//    {
//        return 0;
//    }
//    else
//    {

//    }

//    if(RET(regs) > 0)
//    {
//        // 再拉起一个监控线程去进行监控
//        createMonThread(RET(regs));
//    }
//    else if(!RET(regs))
//    {
//        usleep(100);
//    }
    return 0;
}
